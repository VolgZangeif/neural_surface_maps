checkpointing :
  checkpoint_path    : './outputs/neural_map/'
  prefix : 'overfit'
  suffix : 'XX'

dataset :
  sample_path : '/home/sanyam/working2/neural_surface_maps/outputs/may11_outputs/XX_sample.pth'
  num_workers : 4
  num_points  : 4000
  num_epochs  : 50000

optimizer:
  lr    : 1.0e-4
  momentum      : 0.9

hydra:
  run:
    dir: './'
